# VeriFin Web 演示：验证结论与使用说明

> 生成时间：2026-09-21 | 验证方式：真实启动服务 + 真实 HTTP 请求（非凭文件存在下结论）

## 一、基础功能做完了吗

**核验引擎 + Web 展示页都已做完，且本次实测可运行。**

- 核验引擎（D0–D6）：295 项测试全绿，Track B 29/29、Track D 7/7、gold 三闸门全过。
- Web 展示页（`web/index.html` + `web/server.py`）：**不是待建项，已存在且实测通过**。

实测证据（均来自真实请求，非记忆）：

| 端点 | 实测结果 |
|---|---|
| `GET /api/meta` | 贵州茅台（600519）· 2024 年度 · 334 chunks / 5 表 · embedder=tfidf-local |
| `POST /api/ask`（真实题） | decision=answer；营业收入 170,899,152,276.34；来源第 63 页 · 坐标 (127.1,316.1,420.7,326.6)；坐标校验 verified=True；7 步轨迹全 ok |
| `POST /api/ask`（必拒答） | 「火星基地折旧年限」→ refuse / NO_RECALL（正确） |
| `POST /api/identity` | 资产=负债+权益 本期 PASS，差额 0.00，容差 ±1.5 |
| `POST /api/guard` | 营业收入：真实值采纳 / 篡改值拦截 / 伪造片段拦截（三层全对） |
| `POST /api/agent` | 编排层跑通，返回结构化轨迹（policy 调度离线可跑） |

## 二、使用说明（用户视角）

### 启动（一行）

```bash
cd /Users/shihao/WorkBuddy/2026-09-19-02-37-19
.venv/bin/python -m uvicorn web.server:app --port 8765
# 浏览器打开 http://127.0.0.1:8765
```

> 本机已为你后台启动，现在即可访问上面的地址。

### 页面六大区块

1. **提问与执行轨迹**：输入年报相关问题，右侧实时显示每一步程序判定（四路召回 → RRF → 证据选取 → span 校验 → 坐标定位 → 六元组）。
2. **六元组与证据**：答案的六个字段（公司/期间/指标/数值/单位/来源）+ 原文片段 + **PDF 证据特写图**（坐标层放大裁剪，供人工复核）。
3. **四路召回明细**：每条候选命中了哪几路、各路名次、RRF 融合分。
4. **防幻觉演示台**：同一条证据喂三次（真实值/篡改值/伪造片段），直观看到两层 span 校验怎么拦。
5. **勾稽核验**：资产 = 负债 + 所有者权益，本期/上期双列，给出差额与推导容差。
6. **Agent 编排**：跑一次完整 Agent，逐步看节点/工具/来源，可回放历史运行。

### 可直接试的问题

- `2024年营业收入是多少`
- `2024年资产总计是多少`
- `负债合计`
- `242,011,315,120.60`（直接拿数字反查科目）
- `火星基地折旧年限`（必拒答样本：四路全无命中 → 不猜测、直接拒答）

## 三、已知限制（页面底部也已如实标注）

- 公司名/期间当前来自演示配置（封面结构化解析待接入）；
- 向量通道走本地 TF-IDF（中转端点无 embedding 能力）；
- 坐标层依赖 PDF 内嵌文本层，扫描版需先接 OCR。

## 四、本轮所改

- 修正 README 中过时的「242 项测试」→「295 项测试」（3 处），保持结论与实测一致。
- 未重建 web 页：已存在且实测可用，重复造轮子无意义。

## 五、可增强方向（非基础功能，按需）

- 上传任意 PDF 自动解析 + 建索引（当前演示报告写死为茅台 2024）；
- 多报告切换器；
- 封面结构化解析替换硬编码的 DEMO_COMPANY/DEMO_PERIOD。
